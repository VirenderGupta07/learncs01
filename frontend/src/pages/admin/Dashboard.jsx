export default function AdminDashboard() {
  return (
    <section>
      <h1 className="text-xl font-semibold">Admin Overview</h1>
      <p className="mt-2 text-sm text-gray-500">
        Global metrics (active users, revenue, course counts) render here.
      </p>
    </section>
  );
}
